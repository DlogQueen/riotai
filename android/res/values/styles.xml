<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- candle-dark from the first frame, so there's no white flash before the board -->
    <style name="BoardTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/board_dark</item>
        <item name="android:colorBackground">@color/board_dark</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
